import styles from "../styles"

const Hero = () => {
  return (
    <section id='hero' className={`flex md:flex-row flex-col ${styles.paddingY}`}>
        <div>
            
        </div>
    </section>
  )
}

export default Hero