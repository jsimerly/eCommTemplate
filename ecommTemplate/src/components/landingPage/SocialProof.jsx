

const SocialCard = ({name, headline, review}) => {
    return (
        <div>

        </div>
    )
}

const SocialProof = () => {
  return (
    <div>
        <h3>
            But Don't Take our Word for It
        </h3>
        <div>

        </div>
    </div>
  )
}

export default SocialProof